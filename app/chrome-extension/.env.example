# Chrome Extension Public Key (optional)
# A default dev key is built into wxt.config.ts, so this is only needed
# if you want to override the extension ID (e.g. to match a published extension).
# CHROME_EXTENSION_KEY=YOUR_PUBLIC_KEY_HERE
